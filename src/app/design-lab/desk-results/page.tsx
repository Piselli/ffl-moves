"use client";

import { DeskResultsScene } from "@/components/design-lab/locker-leaderboard/DeskResultsScene";

/** Lab archive — all results chromes. Shipping `/leaderboard` is Crystal only. */
export default function DeskResultsLabPage() {
  return <DeskResultsScene lab />;
}
