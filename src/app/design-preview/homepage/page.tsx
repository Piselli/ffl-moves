import { HomepageNavRedesign } from "@/components/design-preview/HomepageNavRedesign";

export const metadata = {
  title: "Homepage nav redesign",
  robots: { index: false, follow: false },
};

export default function HomepageNavRedesignPage() {
  return <HomepageNavRedesign />;
}
