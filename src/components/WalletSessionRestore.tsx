"use client";

/**
 * Solana Wallet Adapter restores a prior session through `autoConnect`.
 * This compatibility component remains until the legacy wallet files are
 * removed in Phase 5.
 */
export function WalletSessionRestore() {
  return null;
}
