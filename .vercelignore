# Never upload build caches / local toolchains
node_modules
.next
.git
.movement
.numba_cache
.tmp-pydeps
tmp
archive

# Solana local artifacts (huge + unix sockets break upload)
# Anchor workspace only — do NOT use bare `solana` (that also drops src/app/api/solana).
/solana
solana/movematch

# Move local build output
move/**/build

# Marketing boards / refs — not needed for Next app runtime
docs/marketing
